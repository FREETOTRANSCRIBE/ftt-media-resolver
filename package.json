{
  "name": "ftt-media-resolver",
  "version": "1.0.0",
  "private": true,
  "description": "FreeToTranscribe media resolver — pulls audio from a TikTok/Instagram link with yt-dlp + ffmpeg and uploads it to a presigned R2 URL.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
